export enum PAGE_STATUS_ENUM {
  LOADING = 'loading',
  EMPTY = 'empty',
  ERROR = 'error',
  CONTENT = 'content',
  REFRESH = 'refresh'
}

export enum LOAD_ACTION_ENUM {
  REFRESH = 'refresh',
  LOADMORE = 'loadMore',
  RESET = 'reset'
}
